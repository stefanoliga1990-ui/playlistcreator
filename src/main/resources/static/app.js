const DEFAULT_DESCRIPTION = "Playlist generated by playlistcreator";

const state = {
	loggedIn: false,
	preview: null,
	genreArtists: null,
	genreTracks: null,
	discographyAlbums: null,
	discographyTracks: null,
	topTracks: null,
	similarArtists: null,
	similarArtistTracks: null,
	createMode: "setlist",
	activeView: "home",
};

const els = {
	loginStatus: document.querySelector("#loginStatus"),
	logoutButton: document.querySelector("#logoutButton"),
	loginView: document.querySelector("#loginView"),
	homeView: document.querySelector("#homeView"),
	setlistView: document.querySelector("#setlistView"),
	genreView: document.querySelector("#genreView"),
	discographyView: document.querySelector("#discographyView"),
	topTracksView: document.querySelector("#topTracksView"),
	similarArtistsView: document.querySelector("#similarArtistsView"),
	openSetlistView: document.querySelector("#openSetlistView"),
	openGenreView: document.querySelector("#openGenreView"),
	openDiscographyView: document.querySelector("#openDiscographyView"),
	openTopTracksView: document.querySelector("#openTopTracksView"),
	openSimilarArtistsView: document.querySelector("#openSimilarArtistsView"),
	setlistHomeButton: document.querySelector("#setlistHomeButton"),
	genreHomeButton: document.querySelector("#genreHomeButton"),
	discographyHomeButton: document.querySelector("#discographyHomeButton"),
	topTracksHomeButton: document.querySelector("#topTracksHomeButton"),
	similarArtistsHomeButton: document.querySelector("#similarArtistsHomeButton"),
	searchForm: document.querySelector("#searchForm"),
	artistName: document.querySelector("#artistName"),
	message: document.querySelector("#message"),
	results: document.querySelector("#results"),
	artistTitle: document.querySelector("#artistTitle"),
	artistLink: document.querySelector("#artistLink"),
	songCount: document.querySelector("#songCount"),
	setlistCount: document.querySelector("#setlistCount"),
	songList: document.querySelector("#songList"),
	setlistList: document.querySelector("#setlistList"),
	openCreatePanel: document.querySelector("#openCreatePanel"),
	genreArtistForm: document.querySelector("#genreArtistForm"),
	genreName: document.querySelector("#genreName"),
	genreArtistLimit: document.querySelector("#genreArtistLimit"),
	genreMessage: document.querySelector("#genreMessage"),
	genreArtistResults: document.querySelector("#genreArtistResults"),
	genreArtistTitle: document.querySelector("#genreArtistTitle"),
	genreArtistList: document.querySelector("#genreArtistList"),
	genreTrackForm: document.querySelector("#genreTrackForm"),
	tracksPerArtist: document.querySelector("#tracksPerArtist"),
	genreTrackResults: document.querySelector("#genreTrackResults"),
	genreTrackTitle: document.querySelector("#genreTrackTitle"),
	genreArtistCount: document.querySelector("#genreArtistCount"),
	genreTrackCount: document.querySelector("#genreTrackCount"),
	genreTrackList: document.querySelector("#genreTrackList"),
	openGenreCreatePanel: document.querySelector("#openGenreCreatePanel"),
	openGenreCreatePanelBottom: document.querySelector("#openGenreCreatePanelBottom"),
	discographySearchForm: document.querySelector("#discographySearchForm"),
	discographyArtistName: document.querySelector("#discographyArtistName"),
	discographyMessage: document.querySelector("#discographyMessage"),
	discographyAlbumResults: document.querySelector("#discographyAlbumResults"),
	discographyAlbumTitle: document.querySelector("#discographyAlbumTitle"),
	discographyAlbumCount: document.querySelector("#discographyAlbumCount"),
	discographyAlbumList: document.querySelector("#discographyAlbumList"),
	discographyTrackForm: document.querySelector("#discographyTrackForm"),
	discographyTracksPerAlbum: document.querySelector("#discographyTracksPerAlbum"),
	discographyTrackResults: document.querySelector("#discographyTrackResults"),
	discographyTrackTitle: document.querySelector("#discographyTrackTitle"),
	discographySelectedAlbumCount: document.querySelector("#discographySelectedAlbumCount"),
	discographyTrackCount: document.querySelector("#discographyTrackCount"),
	discographyTrackList: document.querySelector("#discographyTrackList"),
	openDiscographyCreatePanel: document.querySelector("#openDiscographyCreatePanel"),
	openDiscographyCreatePanelBottom: document.querySelector("#openDiscographyCreatePanelBottom"),
	topTracksForm: document.querySelector("#topTracksForm"),
	topTracksLimit: document.querySelector("#topTracksLimit"),
	topTracksMonths: document.querySelector("#topTracksMonths"),
	topTracksMessage: document.querySelector("#topTracksMessage"),
	topTracksResults: document.querySelector("#topTracksResults"),
	topTracksTitle: document.querySelector("#topTracksTitle"),
	topTracksRequestedCount: document.querySelector("#topTracksRequestedCount"),
	topTracksAvailableCount: document.querySelector("#topTracksAvailableCount"),
	topTracksPeriod: document.querySelector("#topTracksPeriod"),
	topTracksList: document.querySelector("#topTracksList"),
	openTopTracksCreatePanel: document.querySelector("#openTopTracksCreatePanel"),
	openTopTracksCreatePanelBottom: document.querySelector("#openTopTracksCreatePanelBottom"),
	similarArtistsSearchForm: document.querySelector("#similarArtistsSearchForm"),
	similarArtistsSourceName: document.querySelector("#similarArtistsSourceName"),
	similarArtistsMessage: document.querySelector("#similarArtistsMessage"),
	similarArtistsResults: document.querySelector("#similarArtistsResults"),
	similarArtistsTitle: document.querySelector("#similarArtistsTitle"),
	similarArtistsCount: document.querySelector("#similarArtistsCount"),
	similarArtistsList: document.querySelector("#similarArtistsList"),
	similarArtistsTrackForm: document.querySelector("#similarArtistsTrackForm"),
	similarArtistsTracksPerArtist: document.querySelector("#similarArtistsTracksPerArtist"),
	similarArtistsTrackResults: document.querySelector("#similarArtistsTrackResults"),
	similarArtistsTrackTitle: document.querySelector("#similarArtistsTrackTitle"),
	similarArtistsSelectedCount: document.querySelector("#similarArtistsSelectedCount"),
	similarArtistsTrackCount: document.querySelector("#similarArtistsTrackCount"),
	similarArtistsTrackList: document.querySelector("#similarArtistsTrackList"),
	openSimilarArtistsCreatePanel: document.querySelector("#openSimilarArtistsCreatePanel"),
	openSimilarArtistsCreatePanelBottom: document.querySelector("#openSimilarArtistsCreatePanelBottom"),
	createDialog: document.querySelector("#createDialog"),
	createForm: document.querySelector("#createForm"),
	playlistName: document.querySelector("#playlistName"),
	playlistDescription: document.querySelector("#playlistDescription"),
	closeCreatePanel: document.querySelector("#closeCreatePanel"),
	cancelCreate: document.querySelector("#cancelCreate"),
	loadingOverlay: document.querySelector("#loadingOverlay"),
	loadingTitle: document.querySelector("#loadingTitle"),
	loadingText: document.querySelector("#loadingText"),
};

init();

async function init() {
	console.info("playlistcreator UI loaded");
	bindEvents();
	showLoginHintFromQuery();
	await refreshStatus();
}

function bindEvents() {
	bindSubmit(els.searchForm, async () => {
		await previewArtist(els.artistName.value.trim());
	});

	bindSubmit(els.genreArtistForm, async () => {
		await previewGenreArtists();
	});

	bindSubmit(els.genreTrackForm, async () => {
		await previewGenreTracks();
	});

	bindSubmit(els.discographySearchForm, async () => {
		await previewDiscographyAlbums();
	});

	bindSubmit(els.discographyTrackForm, async () => {
		await previewDiscographyTracks();
	});

	bindSubmit(els.topTracksForm, async () => {
		await previewTopTracks();
	});

	bindSubmit(els.similarArtistsSearchForm, async () => {
		await previewSimilarArtists();
	});

	bindSubmit(els.similarArtistsTrackForm, async () => {
		await previewSimilarArtistTracks();
	});

	bindClick(els.openCreatePanel, () => openSetlistCreateDialog());
	bindClick(els.openGenreCreatePanel, () => openGenreCreateDialog());
	bindClick(els.openGenreCreatePanelBottom, () => openGenreCreateDialog());
	bindClick(els.openDiscographyCreatePanel, () => openDiscographyCreateDialog());
	bindClick(els.openDiscographyCreatePanelBottom, () => openDiscographyCreateDialog());
	bindClick(els.openTopTracksCreatePanel, () => openTopTracksCreateDialog());
	bindClick(els.openTopTracksCreatePanelBottom, () => openTopTracksCreateDialog());
	bindClick(els.openSimilarArtistsCreatePanel, () => openSimilarArtistsCreateDialog());
	bindClick(els.openSimilarArtistsCreatePanelBottom, () => openSimilarArtistsCreateDialog());
	bindClick(els.openSetlistView, () => navigateTo("setlist"));
	bindClick(els.openGenreView, () => navigateTo("genre"));
	bindClick(els.openDiscographyView, () => navigateTo("discography"));
	bindClick(els.openTopTracksView, () => navigateTo("topTracks"));
	bindClick(els.openSimilarArtistsView, () => navigateTo("similarArtists"));
	bindClick(els.setlistHomeButton, () => navigateTo("home"));
	bindClick(els.genreHomeButton, () => navigateTo("home"));
	bindClick(els.discographyHomeButton, () => navigateTo("home"));
	bindClick(els.topTracksHomeButton, () => navigateTo("home"));
	bindClick(els.similarArtistsHomeButton, () => navigateTo("home"));
	bindClick(els.closeCreatePanel, closeCreateDialog);
	bindClick(els.cancelCreate, closeCreateDialog);
	bindClick(els.logoutButton, () => logoutSpotify());

	bindSubmit(els.createForm, async () => {
		await createPlaylist();
	});
}

function bindSubmit(form, handler) {
	if (!form) {
		console.warn("Cannot bind submit handler: form not found");
		return;
	}
	form.addEventListener("submit", async (event) => {
		event.preventDefault();
		event.stopPropagation();
		await handler();
	});
}

function bindClick(element, handler) {
	if (!element) {
		console.warn("Cannot bind click handler: element not found");
		return;
	}
	element.addEventListener("click", handler);
}

async function refreshStatus() {
	try {
		const response = await fetch("/spotify/status");
		const status = await response.json();
		state.loggedIn = Boolean(status.loggedIn);
		renderStatus();
	}
	catch (error) {
		state.loggedIn = false;
		renderStatus();
		showMessage("Non riesco a verificare lo stato del login Spotify.", true);
	}
}

function renderStatus() {
	els.loginStatus.textContent = state.loggedIn ? "Spotify collegato" : "Spotify non collegato";
	els.loginStatus.classList.toggle("ok", state.loggedIn);
	els.logoutButton.classList.toggle("hidden", !state.loggedIn);
	els.loginView.classList.toggle("hidden", state.loggedIn);
	renderActiveView();
}

function navigateTo(view) {
	if (!state.loggedIn) {
		return;
	}
	state.activeView = view;
	renderActiveView();
	window.scrollTo({ top: 0, behavior: "smooth" });
}

function renderActiveView() {
	const showHome = state.loggedIn && state.activeView === "home";
	const showSetlist = state.loggedIn && state.activeView === "setlist";
	const showGenre = state.loggedIn && state.activeView === "genre";
	const showDiscography = state.loggedIn && state.activeView === "discography";
	const showTopTracks = state.loggedIn && state.activeView === "topTracks";
	const showSimilarArtists = state.loggedIn && state.activeView === "similarArtists";
	els.homeView.classList.toggle("hidden", !showHome);
	els.setlistView.classList.toggle("hidden", !showSetlist);
	els.genreView.classList.toggle("hidden", !showGenre);
	els.discographyView.classList.toggle("hidden", !showDiscography);
	els.topTracksView.classList.toggle("hidden", !showTopTracks);
	els.similarArtistsView.classList.toggle("hidden", !showSimilarArtists);
}

function showLoginHintFromQuery() {
	const params = new URLSearchParams(window.location.search);
	if (params.get("spotifyLogin") === "success") {
		window.history.replaceState({}, "", "/");
	}
	if (params.get("spotifyLogin") === "error") {
		showMessage("Login Spotify non completato. Riprova dal pulsante di login.", true);
		window.history.replaceState({}, "", "/");
	}
}

async function previewArtist(artistName) {
	if (!artistName) {
		showMessage("Inserisci il nome di un artista.", true);
		return;
	}
	setBusy(els.searchForm, true);
	hideMessage();
	els.results.classList.add("hidden");
	showLoading("Ricerca scalette", `Recupero delle scalette recenti di ${artistName}...`);
	try {
		const response = await fetch(`/api/playlists/preview?artistName=${encodeURIComponent(artistName)}`);
		const payload = await parseJsonResponse(response);
		state.preview = payload;
		renderPreview(payload);
	}
	catch (error) {
		showMessage(error.message || "Ricerca non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.searchForm, false);
	}
}

function renderPreview(preview) {
	els.artistTitle.textContent = preview.artist.name;
	els.artistLink.href = preview.artist.setlistFmUrl;
	els.songCount.textContent = preview.recentSongsCount;
	els.setlistCount.textContent = preview.sourceSetlists.length;

	els.songList.replaceChildren(...preview.recentSongs.map((song, index) => {
		const item = document.createElement("li");
		item.className = "selectable-result-item";
		const coverText = song.cover ? `Cover: ${song.coverArtist || "artista originale non indicato"}` : "Brano artista";
		item.innerHTML = `
			<input type="checkbox" class="track-selection-checkbox" data-index="${index}" checked aria-label="Includi ${escapeHtml(song.title)}">
			<span class="song-index">${index + 1}</span>
			<span>
				<span class="song-title">${escapeHtml(song.title)}</span>
				<span class="song-meta">${coverText}</span>
			</span>
			<span class="badge">pos. ${song.latestPosition}</span>
		`;
		return item;
	}));

	els.setlistList.replaceChildren(...preview.sourceSetlists.map((setlist) => {
		const card = document.createElement("article");
		card.className = "setlist-card";
		card.innerHTML = `
			<strong>${escapeHtml(formatDate(setlist.eventDate))}</strong>
			<p>${escapeHtml(setlist.venueName || "Venue non indicata")} - ${escapeHtml(setlist.cityName || "")}</p>
			<p>${setlist.songCount} brani - <a href="${setlist.url}" target="_blank" rel="noreferrer">Apri setlist</a></p>
		`;
		return card;
	}));

	els.results.classList.remove("hidden");
}

async function previewGenreArtists() {
	const genre = els.genreName.value.trim();
	const artistLimit = Number(els.genreArtistLimit.value);
	if (!genre) {
		showGenreMessage("Inserisci un genere musicale.", true);
		return;
	}
	setBusy(els.genreArtistForm, true);
	hideGenreMessage();
	els.genreArtistResults.classList.add("hidden");
	els.genreTrackForm.classList.add("hidden");
	els.genreTrackResults.classList.add("hidden");
	state.genreArtists = null;
	state.genreTracks = null;
	showLoading("Ricerca artisti", `Recupero dei principali artisti del genere ${formatGenreName(genre)}...`);
	try {
		const params = new URLSearchParams({
			genre,
			artistLimit: String(artistLimit),
		});
		console.info("Searching Last.fm genre artists", { genre, artistLimit });
		const response = await fetch(`/api/genre-playlists/artists?${params.toString()}`);
		const payload = await parseJsonResponse(response);
		state.genreArtists = payload;
		renderGenreArtists(payload);
	}
	catch (error) {
		showGenreMessage(error.message || "Ricerca artisti non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.genreArtistForm, false);
	}
}

function renderGenreArtists(payload) {
	els.genreArtistTitle.textContent = `${formatGenreName(payload.genre)} - ${payload.artistCount} artisti`;
	els.genreArtistList.replaceChildren(...payload.artists.map((artist) => {
		const item = document.createElement("li");
		item.className = "selectable-result-item";
		item.innerHTML = `
			<input type="checkbox" class="genre-artist-checkbox" data-index="${artist.rank - 1}" checked aria-label="Includi ${escapeHtml(artist.name)}">
			<span class="song-index">${artist.rank}</span>
			<span class="song-title">${escapeHtml(artist.name)}</span>
			${artist.lastFmUrl ? `<a class="compact-link" href="${artist.lastFmUrl}" target="_blank" rel="noreferrer">Last.fm</a>` : ""}
		`;
		return item;
	}));
	els.genreArtistResults.classList.remove("hidden");
	els.genreTrackForm.classList.remove("hidden");
	if (payload.warning) {
		showGenreMessage(payload.warning, false);
	}
}

async function previewGenreTracks() {
	const genre = els.genreName.value.trim();
	const artistLimit = Number(els.genreArtistLimit.value);
	const tracksPerArtist = Number(els.tracksPerArtist.value);
	if (!state.genreArtists) {
		showGenreMessage("Cerca prima gli artisti del genere.", true);
		return;
	}
	const selectedArtistNames = getSelectedGenreArtistNames();
	if (!selectedArtistNames.length) {
		showGenreMessage("Seleziona almeno un artista.", true);
		return;
	}
	setBusy(els.genreTrackForm, true);
	hideGenreMessage();
	els.genreTrackResults.classList.add("hidden");
	showLoading("Ricerca canzoni", "Recupero delle canzoni più ascoltate per gli artisti selezionati...");
	try {
		const response = await fetch("/api/genre-playlists/tracks", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				genre,
				artistLimit,
				selectedArtistNames,
				tracksPerArtist,
			}),
		});
		console.info("Searching Last.fm genre tracks", { genre, artistLimit, tracksPerArtist });
		const payload = await parseJsonResponse(response);
		state.genreTracks = payload;
		renderGenreTracks(payload);
	}
	catch (error) {
		showGenreMessage(error.message || "Ricerca canzoni non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.genreTrackForm, false);
	}
}

function renderGenreTracks(payload) {
	els.genreTrackTitle.textContent = `${formatGenreName(payload.genre)} - Best Of`;
	els.genreArtistCount.textContent = payload.artistCount;
	els.genreTrackCount.textContent = payload.trackCount;
	els.genreTrackList.replaceChildren(...payload.tracks.map((track, index) => {
		const item = document.createElement("li");
		item.className = "selectable-result-item";
		item.innerHTML = `
			<input type="checkbox" class="track-selection-checkbox" data-index="${index}" checked aria-label="Includi ${escapeHtml(track.title)}">
			<span class="song-index">${index + 1}</span>
			<span>
				<span class="song-title">${escapeHtml(track.title)}</span>
				<span class="song-meta">${escapeHtml(track.artistName)} - top ${track.trackRank} artista #${track.artistRank}</span>
			</span>
			<span class="badge">${formatNumber(track.listeners)}</span>
		`;
		return item;
	}));
	els.genreTrackResults.classList.remove("hidden");
}

function getSelectedGenreArtistNames() {
	if (!state.genreArtists) {
		return [];
	}
	return [...els.genreArtistList.querySelectorAll(".genre-artist-checkbox:checked")]
		.map((checkbox) => state.genreArtists.artists[Number(checkbox.dataset.index)])
		.filter(Boolean)
		.map((artist) => artist.name)
		.filter(Boolean);
}

async function previewDiscographyAlbums() {
	const artistName = els.discographyArtistName.value.trim();
	if (!artistName) {
		showDiscographyMessage("Inserisci il nome di un artista.", true);
		return;
	}
	setBusy(els.discographySearchForm, true);
	hideDiscographyMessage();
	els.discographyAlbumResults.classList.add("hidden");
	els.discographyTrackForm.classList.add("hidden");
	els.discographyTrackResults.classList.add("hidden");
	state.discographyAlbums = null;
	state.discographyTracks = null;
	showLoading("Ricerca album", `Recupero della discografia di ${artistName}...`);
	try {
		const response = await fetch(`/api/discography-playlists/albums?artistName=${encodeURIComponent(artistName)}`);
		const payload = await parseJsonResponse(response);
		state.discographyAlbums = payload;
		renderDiscographyAlbums(payload);
	}
	catch (error) {
		showDiscographyMessage(error.message || "Ricerca album non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.discographySearchForm, false);
	}
}

function renderDiscographyAlbums(payload) {
	els.discographyArtistName.value = payload.artist.name;
	els.discographyAlbumTitle.textContent = payload.artist.name;
	els.discographyAlbumCount.textContent = `${payload.albumCount} album`;
	els.discographyAlbumList.replaceChildren(...payload.albums.map((album) => {
		const item = document.createElement("label");
		item.className = "album-selection-item";
		item.innerHTML = `
			<input type="checkbox" class="discography-album-checkbox" value="${escapeHtml(album.id)}" checked>
			<span class="album-year">${album.releaseYear}</span>
			<span class="album-name">${escapeHtml(album.name)}</span>
			<span class="album-track-count">${album.totalTracks} brani</span>
		`;
		return item;
	}));
	els.discographyAlbumResults.classList.remove("hidden");
	els.discographyTrackForm.classList.remove("hidden");
	if (payload.filteredAlbumCount > 0) {
		showDiscographyMessage(
			`${payload.filteredAlbumCount} album live, raccolte o edizioni duplicate sono stati esclusi automaticamente.`,
			false);
	}
}

async function previewDiscographyTracks() {
	if (!state.discographyAlbums) {
		showDiscographyMessage("Cerca prima gli album dell'artista.", true);
		return;
	}
	const includedAlbumIds = getSelectedDiscographyAlbumIds();
	if (includedAlbumIds.length === 0) {
		showDiscographyMessage("Seleziona almeno un album.", true);
		return;
	}
	const tracksPerAlbum = Number(els.discographyTracksPerAlbum.value);
	setBusy(els.discographyTrackForm, true);
	hideDiscographyMessage();
	els.discographyTrackResults.classList.add("hidden");
	showLoading("Creazione lista di canzoni", "Analisi dei brani più ascoltati per ogni album selezionato...");
	try {
		const response = await fetch("/api/discography-playlists/tracks", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				artistId: state.discographyAlbums.artist.id,
				artistName: state.discographyAlbums.artist.name,
				includedAlbumIds,
				tracksPerAlbum,
			}),
		});
		const payload = await parseJsonResponse(response);
		state.discographyTracks = payload;
		renderDiscographyTracks(payload);
	}
	catch (error) {
		showDiscographyMessage(error.message || "Creazione lista di canzoni non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.discographyTrackForm, false);
	}
}

function renderDiscographyTracks(payload) {
	els.discographyTrackTitle.textContent = `${payload.artistName} - Discografia essenziale`;
	els.discographySelectedAlbumCount.textContent = payload.albumCount;
	els.discographyTrackCount.textContent = payload.trackCount;
	let trackIndex = 0;
	els.discographyTrackList.replaceChildren(...payload.albums.map((album) => {
		const group = document.createElement("section");
		group.className = "discography-album-group";
		const heading = document.createElement("h3");
		heading.innerHTML = `<span class="discography-album-heading">${album.releaseYear}</span> ${escapeHtml(album.name)}`;
		const list = document.createElement("ol");
		list.className = "song-list";
		list.replaceChildren(...album.tracks.map((track) => {
			const item = document.createElement("li");
			item.className = "selectable-result-item";
			const currentIndex = trackIndex++;
			const rankingText = track.rankingSource === "LAST_FM"
				? `${formatNumber(track.lastFmPlaycount)} ascolti Last.fm`
				: "ordine originale dell'album";
			item.innerHTML = `
				<input type="checkbox" class="track-selection-checkbox" data-index="${currentIndex}" checked aria-label="Includi ${escapeHtml(track.title)}">
				<span class="song-index">${track.selectedRank}</span>
				<span>
					<span class="song-title">${escapeHtml(track.title)}</span>
					<span class="song-meta">${rankingText}</span>
				</span>
				<span class="badge">${track.rankingSource === "LAST_FM" ? "Last.fm" : "Album"}</span>
			`;
			return item;
		}));
		group.append(heading, list);
		return group;
	}));
	els.discographyTrackResults.classList.remove("hidden");
}

function getSelectedDiscographyAlbumIds() {
	return [...els.discographyAlbumList.querySelectorAll(".discography-album-checkbox:checked")]
		.map((checkbox) => checkbox.value);
}

async function previewTopTracks() {
	const trackLimit = Number(els.topTracksLimit.value);
	const months = Number(els.topTracksMonths.value);
	setBusy(els.topTracksForm, true);
	hideTopTracksMessage();
	els.topTracksResults.classList.add("hidden");
	state.topTracks = null;
	showLoading("Ricerca brani preferiti", `Recupero dei tuoi Top ${trackLimit} brani Spotify...`);
	try {
		const params = new URLSearchParams({
			trackLimit: String(trackLimit),
			months: String(months),
		});
		const response = await fetch(`/api/top-tracks-playlists/preview?${params.toString()}`);
		const payload = await parseJsonResponse(response);
		state.topTracks = payload;
		renderTopTracks(payload);
		if (payload.warning) {
			showTopTracksMessage(payload.warning, false);
		}
	}
	catch (error) {
		showTopTracksMessage(error.message || "Ricerca dei brani preferiti non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.topTracksForm, false);
	}
}

function renderTopTracks(payload) {
	els.topTracksTitle.textContent = `Top ${payload.availableTrackCount} - ${formatPeriod(payload.months)}`;
	els.topTracksRequestedCount.textContent = payload.requestedTrackCount;
	els.topTracksAvailableCount.textContent = payload.availableTrackCount;
	els.topTracksPeriod.textContent = formatPeriod(payload.months);
	els.topTracksList.replaceChildren(...payload.tracks.map((track) => {
		const item = document.createElement("li");
		item.className = "selectable-result-item";
		const artists = Array.isArray(track.artists) ? track.artists.join(", ") : "";
		item.innerHTML = `
			<input type="checkbox" class="track-selection-checkbox" data-index="${track.rank - 1}" checked aria-label="Includi ${escapeHtml(track.title)}">
			<span class="song-index">${track.rank}</span>
			<span>
				<span class="song-title">${escapeHtml(track.title)}</span>
				<span class="song-meta">${escapeHtml(artists)}${track.albumName ? ` - ${escapeHtml(track.albumName)}` : ""}</span>
			</span>
			<span class="badge">Spotify</span>
		`;
		return item;
	}));
	els.topTracksResults.classList.remove("hidden");
}

async function previewSimilarArtists() {
	const artistName = els.similarArtistsSourceName.value.trim();
	if (!artistName) {
		showSimilarArtistsMessage("Inserisci il nome di un artista.", true);
		return;
	}
	setBusy(els.similarArtistsSearchForm, true);
	hideSimilarArtistsMessage();
	els.similarArtistsResults.classList.add("hidden");
	els.similarArtistsTrackForm.classList.add("hidden");
	els.similarArtistsTrackResults.classList.add("hidden");
	state.similarArtists = null;
	state.similarArtistTracks = null;
	showLoading(
		"Ricerca artisti simili",
		`Analisi della similarità, dei tag e delle relazioni reciproche per ${artistName}...`);
	try {
		const response = await fetch(
			`/api/similar-artists-playlists/artists?artistName=${encodeURIComponent(artistName)}`);
		const payload = await parseJsonResponse(response);
		state.similarArtists = payload;
		renderSimilarArtists(payload);
		if (payload.warning) {
			showSimilarArtistsMessage(payload.warning, false);
		}
	}
	catch (error) {
		showSimilarArtistsMessage(error.message || "Ricerca degli artisti simili non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.similarArtistsSearchForm, false);
	}
}

function renderSimilarArtists(payload) {
	els.similarArtistsSourceName.value = payload.sourceArtistName;
	els.similarArtistsTitle.textContent = `Artisti simili a ${payload.sourceArtistName}`;
	els.similarArtistsCount.textContent = `${payload.artistCount} artisti`;
	els.similarArtistsList.replaceChildren(...payload.artists.map((artist) => {
		const item = document.createElement("label");
		item.className = "similar-artist-selection-item";
		const tags = Array.isArray(artist.topTags) && artist.topTags.length
			? artist.topTags.join(", ")
			: "Tag non disponibili";
		item.innerHTML = `
			<input type="checkbox" class="similar-artist-checkbox" value="${escapeHtml(artist.name)}" checked>
			<span class="song-index">${artist.rank}</span>
			<span class="similar-artist-details">
				<span class="song-title">${escapeHtml(artist.name)}</span>
				<span class="similar-artist-tags">${escapeHtml(tags)}</span>
			</span>
			<span class="similarity-signals">
				<span>Diretta ${formatPercent(artist.directSimilarity)}</span>
				<span>Reciproca ${formatPercent(artist.reciprocalSimilarity)}</span>
				<span>Tag ${formatPercent(artist.tagSimilarity)}</span>
			</span>
			<span class="similarity-score">${artist.similarityScore}/100</span>
		`;
		return item;
	}));
	els.similarArtistsResults.classList.remove("hidden");
	els.similarArtistsTrackForm.classList.remove("hidden");
}

async function previewSimilarArtistTracks() {
	if (!state.similarArtists) {
		showSimilarArtistsMessage("Cerca prima gli artisti simili.", true);
		return;
	}
	const selectedArtistNames = getSelectedSimilarArtistNames();
	if (!selectedArtistNames.length) {
		showSimilarArtistsMessage("Seleziona almeno un artista.", true);
		return;
	}
	const tracksPerArtist = Number(els.similarArtistsTracksPerArtist.value);
	setBusy(els.similarArtistsTrackForm, true);
	hideSimilarArtistsMessage();
	els.similarArtistsTrackResults.classList.add("hidden");
	state.similarArtistTracks = null;
	showLoading(
		"Ricerca canzoni",
		`Recupero delle top ${tracksPerArtist} canzoni per ${selectedArtistNames.length} artisti...`);
	try {
		const response = await fetch("/api/similar-artists-playlists/tracks", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				sourceArtistName: state.similarArtists.sourceArtistName,
				selectedArtistNames,
				tracksPerArtist,
			}),
		});
		const payload = await parseJsonResponse(response);
		state.similarArtistTracks = payload;
		renderSimilarArtistTracks(payload);
		if (payload.warning) {
			showSimilarArtistsMessage(payload.warning, false);
		}
	}
	catch (error) {
		showSimilarArtistsMessage(error.message || "Ricerca delle canzoni non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.similarArtistsTrackForm, false);
	}
}

function renderSimilarArtistTracks(payload) {
	els.similarArtistsTrackTitle.textContent = `${payload.sourceArtistName} - Artisti simili`;
	els.similarArtistsSelectedCount.textContent = payload.artistCount;
	els.similarArtistsTrackCount.textContent = payload.trackCount;
	els.similarArtistsTrackList.replaceChildren(...payload.tracks.map((track, index) => {
		const item = document.createElement("li");
		item.className = "selectable-result-item";
		item.innerHTML = `
			<input type="checkbox" class="track-selection-checkbox" data-index="${index}" checked aria-label="Includi ${escapeHtml(track.title)}">
			<span class="song-index">${index + 1}</span>
			<span>
				<span class="song-title">${escapeHtml(track.title)}</span>
				<span class="song-meta">${escapeHtml(track.artistName)} - top ${track.trackRank}</span>
			</span>
			<span class="badge">${formatNumber(track.listeners)} ascoltatori</span>
		`;
		return item;
	}));
	els.similarArtistsTrackResults.classList.remove("hidden");
}

function getSelectedSimilarArtistNames() {
	return [...els.similarArtistsList.querySelectorAll(".similar-artist-checkbox:checked")]
		.map((checkbox) => checkbox.value);
}

function getSelectedTrackRequests(container, tracks, mapper) {
	return [...container.querySelectorAll(".track-selection-checkbox:checked")]
		.map((checkbox) => tracks[Number(checkbox.dataset.index)])
		.filter(Boolean)
		.map(mapper);
}

function flattenDiscographyTracks() {
	return state.discographyTracks
		? state.discographyTracks.albums.flatMap((album) => album.tracks)
		: [];
}

function openSetlistCreateDialog() {
	if (!state.preview) {
		showMessage("Cerca prima un artista.", true);
		return;
	}
	if (!getSelectedSetlistTracks().length) {
		showMessage("Seleziona almeno un brano.", true);
		return;
	}
	state.createMode = "setlist";
	els.playlistName.value = `${state.preview.artist.name} - Recent setlists`;
	els.playlistDescription.value = DEFAULT_DESCRIPTION;
	openCreateDialog();
}

function openGenreCreateDialog() {
	if (!state.genreTracks) {
		showGenreMessage("Cerca prima le canzoni del genere.", true);
		return;
	}
	if (!getSelectedGenreTracks().length) {
		showGenreMessage("Seleziona almeno un brano.", true);
		return;
	}
	state.createMode = "genre";
	els.playlistName.value = `${formatGenreName(state.genreTracks.genre)} - Best Of`;
	els.playlistDescription.value = `Playlist generated by playlistcreator from Last.fm ${formatGenreName(state.genreTracks.genre)} charts`;
	openCreateDialog();
}

function openDiscographyCreateDialog() {
	if (!state.discographyTracks) {
		showDiscographyMessage("Crea prima la lista di canzoni.", true);
		return;
	}
	if (!getSelectedDiscographyTracks().length) {
		showDiscographyMessage("Seleziona almeno un brano.", true);
		return;
	}
	state.createMode = "discography";
	els.playlistName.value = `${state.discographyTracks.artistName} - Discografia essenziale`;
	els.playlistDescription.value =
		`Playlist generated by playlistcreator from the essential discography of ${state.discographyTracks.artistName}`;
	openCreateDialog();
}

function openTopTracksCreateDialog() {
	if (!state.topTracks) {
		showTopTracksMessage("Cerca prima i tuoi brani preferiti.", true);
		return;
	}
	if (!getSelectedTopTracks().length) {
		showTopTracksMessage("Seleziona almeno un brano.", true);
		return;
	}
	state.createMode = "topTracks";
	els.playlistName.value =
		`I miei Top ${state.topTracks.availableTrackCount} - ${formatPeriod(state.topTracks.months)}`;
	els.playlistDescription.value = "Playlist generated by playlistcreator from my Spotify top tracks";
	openCreateDialog();
}

function openSimilarArtistsCreateDialog() {
	if (!state.similarArtistTracks) {
		showSimilarArtistsMessage("Cerca prima le canzoni degli artisti simili.", true);
		return;
	}
	if (!getSelectedSimilarArtistTracks().length) {
		showSimilarArtistsMessage("Seleziona almeno un brano.", true);
		return;
	}
	state.createMode = "similarArtists";
	els.playlistName.value = `${state.similarArtistTracks.sourceArtistName} - Artisti simili`;
	els.playlistDescription.value =
		`Playlist generated by playlistcreator from artists similar to ${state.similarArtistTracks.sourceArtistName}`;
	openCreateDialog();
}

function getSelectedSetlistTracks() {
	return state.preview
		? getSelectedTrackRequests(els.songList, state.preview.recentSongs, (track) => ({
			id: null,
			artistName: state.preview.artist.name,
			title: track.title,
		}))
		: [];
}

function getSelectedGenreTracks() {
	return state.genreTracks
		? getSelectedTrackRequests(els.genreTrackList, state.genreTracks.tracks, (track) => ({
			id: null,
			artistName: track.artistName,
			title: track.title,
		}))
		: [];
}

function getSelectedDiscographyTracks() {
	return getSelectedTrackRequests(
		els.discographyTrackList,
		flattenDiscographyTracks(),
		(track) => ({
			id: track.id,
			artistName: state.discographyTracks.artistName,
			title: track.title,
		}));
}

function getSelectedTopTracks() {
	return state.topTracks
		? getSelectedTrackRequests(els.topTracksList, state.topTracks.tracks, (track) => ({
			id: track.id,
			artistName: Array.isArray(track.artists) ? track.artists.join(", ") : null,
			title: track.title,
		}))
		: [];
}

function getSelectedSimilarArtistTracks() {
	return state.similarArtistTracks
		? getSelectedTrackRequests(
			els.similarArtistsTrackList,
			state.similarArtistTracks.tracks,
			(track) => ({
				id: null,
				artistName: track.artistName,
				title: track.title,
			}))
		: [];
}

function openCreateDialog() {
	els.createDialog.classList.remove("hidden");
	els.playlistName.focus();
}

function closeCreateDialog() {
	els.createDialog.classList.add("hidden");
}

async function logoutSpotify() {
	setBusy(els.logoutButton.parentElement, true);
	try {
		await fetch("/spotify/logout", { method: "POST" });
		resetWorkspace();
		state.loggedIn = false;
		state.activeView = "home";
		renderStatus();
		window.history.replaceState({}, "", "/");
	}
	catch (error) {
		showMessage("Logout non riuscito. Riprova.", true);
	}
	finally {
		setBusy(els.logoutButton.parentElement, false);
	}
}

function resetWorkspace() {
	resetSetlistSection();
	resetGenreSection();
	resetDiscographySection();
	resetTopTracksSection();
	resetSimilarArtistsSection();
	state.createMode = "setlist";
	state.activeView = "home";
	closeCreateDialog();
}

function resetSetlistSection() {
	state.preview = null;
	els.artistName.value = "";
	hideMessage();
	els.results.classList.add("hidden");
	els.artistTitle.textContent = "";
	els.artistLink.removeAttribute("href");
	els.songCount.textContent = "0";
	els.setlistCount.textContent = "0";
	els.songList.replaceChildren();
	els.setlistList.replaceChildren();
}

function resetGenreSection() {
	state.genreArtists = null;
	state.genreTracks = null;
	els.genreName.value = "";
	els.genreArtistLimit.value = "15";
	els.tracksPerArtist.value = "3";
	hideGenreMessage();
	els.genreArtistResults.classList.add("hidden");
	els.genreTrackForm.classList.add("hidden");
	els.genreTrackResults.classList.add("hidden");
	els.genreArtistTitle.textContent = "";
	els.genreArtistList.replaceChildren();
	els.genreTrackTitle.textContent = "";
	els.genreArtistCount.textContent = "0";
	els.genreTrackCount.textContent = "0";
	els.genreTrackList.replaceChildren();
}

function resetDiscographySection() {
	state.discographyAlbums = null;
	state.discographyTracks = null;
	els.discographyArtistName.value = "";
	els.discographyTracksPerAlbum.value = "2";
	hideDiscographyMessage();
	els.discographyAlbumResults.classList.add("hidden");
	els.discographyTrackForm.classList.add("hidden");
	els.discographyTrackResults.classList.add("hidden");
	els.discographyAlbumTitle.textContent = "";
	els.discographyAlbumCount.textContent = "0 album";
	els.discographyAlbumList.replaceChildren();
	els.discographyTrackTitle.textContent = "";
	els.discographySelectedAlbumCount.textContent = "0";
	els.discographyTrackCount.textContent = "0";
	els.discographyTrackList.replaceChildren();
}

function resetTopTracksSection() {
	state.topTracks = null;
	els.topTracksLimit.value = "25";
	els.topTracksMonths.value = "6";
	hideTopTracksMessage();
	els.topTracksResults.classList.add("hidden");
	els.topTracksTitle.textContent = "";
	els.topTracksRequestedCount.textContent = "0";
	els.topTracksAvailableCount.textContent = "0";
	els.topTracksPeriod.textContent = "";
	els.topTracksList.replaceChildren();
}

function resetSimilarArtistsSection() {
	state.similarArtists = null;
	state.similarArtistTracks = null;
	els.similarArtistsSourceName.value = "";
	els.similarArtistsTracksPerArtist.value = "3";
	hideSimilarArtistsMessage();
	els.similarArtistsResults.classList.add("hidden");
	els.similarArtistsTrackForm.classList.add("hidden");
	els.similarArtistsTrackResults.classList.add("hidden");
	els.similarArtistsTitle.textContent = "";
	els.similarArtistsCount.textContent = "0 artisti";
	els.similarArtistsList.replaceChildren();
	els.similarArtistsTrackTitle.textContent = "";
	els.similarArtistsSelectedCount.textContent = "0";
	els.similarArtistsTrackCount.textContent = "0";
	els.similarArtistsTrackList.replaceChildren();
}

async function createPlaylist() {
	if (state.createMode === "genre") {
		await createGenrePlaylist();
		return;
	}
	if (state.createMode === "discography") {
		await createDiscographyPlaylist();
		return;
	}
	if (state.createMode === "topTracks") {
		await createTopTracksPlaylist();
		return;
	}
	if (state.createMode === "similarArtists") {
		await createSimilarArtistsPlaylist();
		return;
	}
	await createSetlistPlaylist();
}

async function createSetlistPlaylist() {
	if (!state.preview) {
		showMessage("Cerca prima un artista.", true);
		closeCreateDialog();
		return;
	}
	const selectedTracks = getSelectedSetlistTracks();
	if (!selectedTracks.length) {
		showMessage("Seleziona almeno un brano.", true);
		closeCreateDialog();
		return;
	}
	setBusy(els.createForm, true);
	showLoading("Creazione playlist", `Creazione di "${els.playlistName.value.trim() || "playlist"}" in corso...`);
	try {
		const response = await fetch("/api/playlists/generate", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				artistName: state.preview.artist.name,
				selectedTracks,
				playlistName: els.playlistName.value.trim(),
				playlistDescription: els.playlistDescription.value.trim(),
			}),
		});
		const payload = await parseJsonResponse(response);
		closeCreateDialog();
		resetGenreSection();
		resetDiscographySection();
		resetTopTracksSection();
		resetSimilarArtistsSection();
		showPlaylistCreated(
			els.message,
			payload.playlist.name || els.playlistName.value.trim(),
			payload.playlist.spotifyUrl,
			payload.excludedTracks);
	}
	catch (error) {
		showMessage(error.message || "Creazione playlist non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.createForm, false);
	}
}

async function createGenrePlaylist() {
	if (!state.genreTracks) {
		showGenreMessage("Cerca prima le canzoni del genere.", true);
		closeCreateDialog();
		return;
	}
	const selectedTracks = getSelectedGenreTracks();
	if (!selectedTracks.length) {
		showGenreMessage("Seleziona almeno un brano.", true);
		closeCreateDialog();
		return;
	}
	setBusy(els.createForm, true);
	showLoading("Creazione playlist", `Creazione di "${els.playlistName.value.trim() || "playlist"}" in corso...`);
	try {
		const response = await fetch("/api/genre-playlists/generate", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				genre: state.genreTracks.genre,
				artistLimit: Number(els.genreArtistLimit.value),
				selectedArtistNames: state.genreTracks.artists.map((artist) => artist.name),
				tracksPerArtist: Number(els.tracksPerArtist.value),
				selectedTracks,
				playlistName: els.playlistName.value.trim(),
				playlistDescription: els.playlistDescription.value.trim(),
			}),
		});
		const payload = await parseJsonResponse(response);
		closeCreateDialog();
		resetSetlistSection();
		resetDiscographySection();
		resetTopTracksSection();
		resetSimilarArtistsSection();
		showPlaylistCreated(
			els.genreMessage,
			payload.playlist.name || els.playlistName.value.trim(),
			payload.playlist.spotifyUrl,
			payload.excludedTracks);
	}
	catch (error) {
		showGenreMessage(error.message || "Creazione playlist non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.createForm, false);
	}
}

async function createDiscographyPlaylist() {
	if (!state.discographyTracks || !state.discographyAlbums) {
		showDiscographyMessage("Crea prima la lista di canzoni.", true);
		closeCreateDialog();
		return;
	}
	const selectedTracks = getSelectedDiscographyTracks();
	if (!selectedTracks.length) {
		showDiscographyMessage("Seleziona almeno un brano.", true);
		closeCreateDialog();
		return;
	}
	setBusy(els.createForm, true);
	showLoading("Creazione playlist", `Creazione di "${els.playlistName.value.trim() || "playlist"}" in corso...`);
	try {
		const response = await fetch("/api/discography-playlists/generate", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				artistId: state.discographyAlbums.artist.id,
				artistName: state.discographyTracks.artistName,
				includedAlbumIds: state.discographyTracks.albums.map((album) => album.id),
				tracksPerAlbum: state.discographyTracks.tracksPerAlbum,
				selectedTracks,
				playlistName: els.playlistName.value.trim(),
				playlistDescription: els.playlistDescription.value.trim(),
			}),
		});
		const payload = await parseJsonResponse(response);
		closeCreateDialog();
		resetSetlistSection();
		resetGenreSection();
		resetTopTracksSection();
		resetSimilarArtistsSection();
		showPlaylistCreated(
			els.discographyMessage,
			payload.playlist.name || els.playlistName.value.trim(),
			payload.playlist.spotifyUrl,
			payload.excludedTracks);
	}
	catch (error) {
		showDiscographyMessage(error.message || "Creazione playlist non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.createForm, false);
	}
}

async function createTopTracksPlaylist() {
	if (!state.topTracks) {
		showTopTracksMessage("Cerca prima i tuoi brani preferiti.", true);
		closeCreateDialog();
		return;
	}
	const selectedTracks = getSelectedTopTracks();
	if (!selectedTracks.length) {
		showTopTracksMessage("Seleziona almeno un brano.", true);
		closeCreateDialog();
		return;
	}
	setBusy(els.createForm, true);
	showLoading("Creazione playlist", `Creazione di "${els.playlistName.value.trim() || "playlist"}" in corso...`);
	try {
		const response = await fetch("/api/top-tracks-playlists/generate", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				trackLimit: state.topTracks.requestedTrackCount,
				months: state.topTracks.months,
				selectedTracks,
				playlistName: els.playlistName.value.trim(),
				playlistDescription: els.playlistDescription.value.trim(),
			}),
		});
		const payload = await parseJsonResponse(response);
		closeCreateDialog();
		resetSetlistSection();
		resetGenreSection();
		resetDiscographySection();
		resetSimilarArtistsSection();
		showPlaylistCreated(
			els.topTracksMessage,
			payload.playlist.name || els.playlistName.value.trim(),
			payload.playlist.spotifyUrl,
			payload.excludedTracks);
	}
	catch (error) {
		showTopTracksMessage(error.message || "Creazione playlist non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.createForm, false);
	}
}

async function createSimilarArtistsPlaylist() {
	if (!state.similarArtists || !state.similarArtistTracks) {
		showSimilarArtistsMessage("Cerca prima le canzoni degli artisti simili.", true);
		closeCreateDialog();
		return;
	}
	const selectedTracks = getSelectedSimilarArtistTracks();
	if (!selectedTracks.length) {
		showSimilarArtistsMessage("Seleziona almeno un brano.", true);
		closeCreateDialog();
		return;
	}
	setBusy(els.createForm, true);
	showLoading("Creazione playlist", `Creazione di "${els.playlistName.value.trim() || "playlist"}" in corso...`);
	try {
		const response = await fetch("/api/similar-artists-playlists/generate", {
			method: "POST",
			headers: { "Content-Type": "application/json" },
			body: JSON.stringify({
				sourceArtistName: state.similarArtistTracks.sourceArtistName,
				selectedArtistNames: state.similarArtistTracks.artists.map((artist) => artist.name),
				tracksPerArtist: state.similarArtistTracks.tracksPerArtist,
				selectedTracks,
				playlistName: els.playlistName.value.trim(),
				playlistDescription: els.playlistDescription.value.trim(),
			}),
		});
		const payload = await parseJsonResponse(response);
		closeCreateDialog();
		resetSetlistSection();
		resetGenreSection();
		resetDiscographySection();
		resetTopTracksSection();
		showPlaylistCreated(
			els.similarArtistsMessage,
			payload.playlist.name || els.playlistName.value.trim(),
			payload.playlist.spotifyUrl,
			payload.excludedTracks);
	}
	catch (error) {
		showSimilarArtistsMessage(error.message || "Creazione playlist non riuscita.", true);
	}
	finally {
		hideLoading();
		setBusy(els.createForm, false);
	}
}

function showPlaylistCreated(container, playlistName, playlistUrl, excludedTracks) {
	removeExcludedTracksMessage(container);
	container.classList.remove("error");
	container.classList.add("success");
	container.classList.remove("hidden");
	container.replaceChildren(
			document.createTextNode(`Playlist ${playlistName || "Spotify"} creata correttamente`));
	if (playlistUrl) {
		const link = document.createElement("a");
		link.className = "playlist-link";
		link.href = playlistUrl;
		link.target = "_blank";
		link.rel = "noreferrer";
		link.textContent = playlistUrl;
		container.appendChild(link);
	}
	const excluded = [...new Set(Array.isArray(excludedTracks) ? excludedTracks.filter(Boolean) : [])];
	if (excluded.length) {
		const warning = document.createElement("section");
		warning.className = "message playlist-excluded-warning";
		const title = document.createElement("p");
		title.textContent = "I seguenti brani non sono stati aggiunti alla playlist:";
		const list = document.createElement("ul");
		list.replaceChildren(...excluded.map((track) => {
			const item = document.createElement("li");
			item.textContent = track;
			return item;
		}));
		const reason = document.createElement("p");
		reason.className = "excluded-tracks-reason";
		reason.textContent = "Nessuna corrispondenza affidabile trovata.";
		warning.append(title, list, reason);
		container.insertAdjacentElement("afterend", warning);
	}
	window.scrollTo({ top: 0, behavior: "smooth" });
}

function removeExcludedTracksMessage(container) {
	const warning = container.parentElement
		? container.parentElement.querySelector(".playlist-excluded-warning")
		: null;
	if (warning) {
		warning.remove();
	}
}

async function parseJsonResponse(response) {
	const payload = await response.json().catch(() => ({}));
	if (!response.ok) {
		throw new Error(payload.message || `Errore HTTP ${response.status}`);
	}
	return payload;
}

function showMessage(text, isError) {
	els.message.textContent = text;
	els.message.classList.remove("success");
	els.message.classList.toggle("error", isError);
	els.message.classList.remove("hidden");
}

function hideMessage() {
	removeExcludedTracksMessage(els.message);
	els.message.classList.add("hidden");
	els.message.textContent = "";
}

function showGenreMessage(text, isError) {
	els.genreMessage.textContent = text;
	els.genreMessage.classList.remove("success");
	els.genreMessage.classList.toggle("error", isError);
	els.genreMessage.classList.remove("hidden");
}

function hideGenreMessage() {
	removeExcludedTracksMessage(els.genreMessage);
	els.genreMessage.classList.add("hidden");
	els.genreMessage.textContent = "";
}

function showDiscographyMessage(text, isError) {
	els.discographyMessage.textContent = text;
	els.discographyMessage.classList.remove("success");
	els.discographyMessage.classList.toggle("error", isError);
	els.discographyMessage.classList.remove("hidden");
}

function hideDiscographyMessage() {
	removeExcludedTracksMessage(els.discographyMessage);
	els.discographyMessage.classList.add("hidden");
	els.discographyMessage.textContent = "";
}

function showTopTracksMessage(text, isError) {
	els.topTracksMessage.textContent = text;
	els.topTracksMessage.classList.remove("success");
	els.topTracksMessage.classList.toggle("error", isError);
	els.topTracksMessage.classList.remove("hidden");
}

function hideTopTracksMessage() {
	removeExcludedTracksMessage(els.topTracksMessage);
	els.topTracksMessage.classList.add("hidden");
	els.topTracksMessage.textContent = "";
}

function showSimilarArtistsMessage(text, isError) {
	els.similarArtistsMessage.textContent = text;
	els.similarArtistsMessage.classList.remove("success");
	els.similarArtistsMessage.classList.toggle("error", isError);
	els.similarArtistsMessage.classList.remove("hidden");
}

function hideSimilarArtistsMessage() {
	removeExcludedTracksMessage(els.similarArtistsMessage);
	els.similarArtistsMessage.classList.add("hidden");
	els.similarArtistsMessage.textContent = "";
}

function setBusy(form, busy) {
	form.querySelectorAll("button, input, select, textarea").forEach((element) => {
		element.disabled = busy;
	});
}

function showLoading(title, text) {
	els.loadingTitle.textContent = title;
	els.loadingText.textContent = text;
	els.loadingOverlay.classList.remove("hidden");
}

function hideLoading() {
	els.loadingOverlay.classList.add("hidden");
}

function formatDate(value) {
	if (!value) {
		return "Data non indicata";
	}
	return new Intl.DateTimeFormat("it-IT", { dateStyle: "medium" }).format(new Date(`${value}T00:00:00`));
}

function formatNumber(value) {
	return new Intl.NumberFormat("it-IT").format(Number(value || 0));
}

function formatGenreName(value) {
	return String(value || "")
		.trim()
		.split(/\s+/)
		.filter(Boolean)
		.map((token) => token.charAt(0).toUpperCase() + token.slice(1).toLowerCase())
		.join(" ");
}

function formatPeriod(months) {
	return Number(months) === 1 ? "Ultimo mese" : `Ultimi ${months} mesi`;
}

function formatPercent(value) {
	return `${Math.round(Number(value || 0) * 100)}%`;
}

function escapeHtml(value) {
	return String(value === null || value === undefined ? "" : value)
		.replaceAll("&", "&amp;")
		.replaceAll("<", "&lt;")
		.replaceAll(">", "&gt;")
		.replaceAll('"', "&quot;")
		.replaceAll("'", "&#039;");
}
